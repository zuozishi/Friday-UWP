<Page
    x:Class="Friday.Views.PicsViewPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Friday.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="Black">
        <AppBarButton Click="GoBack" Width="50" Height="50" Icon="Back" Foreground="White"></AppBarButton>
        <AppBarButton Click="SaveImage" Width="50" Height="50" Icon="Save" Foreground="White" HorizontalAlignment="Right"></AppBarButton>
        <FlipView x:Name="flipview" Margin="0,50"></FlipView>
        <Grid Height="50" VerticalAlignment="Bottom">
            <Border x:Name="grid_GG" Height="auto" Visibility="Visible" VerticalAlignment="Center" CornerRadius="10" HorizontalAlignment="Center" Background="#7F000000" >
                <TextBlock x:Name="txt_GG" Foreground="White" VerticalAlignment="Center" Margin="10,5"><Run x:Name="thispicnum"></Run>/<Run x:Name="picsnum"></Run></TextBlock>
            </Border>
        </Grid>
    </Grid>
</Page>
